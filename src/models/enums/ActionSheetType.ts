export enum DefaultMenuType {
  cancel,
  selectDefaultImage,
  selectFromPhotoLibrary,
  selectFromCamera,
}

export enum ImageUploadMenuType {
  cancel,
  selectFromPhotoLibrary,
  selectFromCamera,
}
